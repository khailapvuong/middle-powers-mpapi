# M-PAPI Python dependencies.
#
# Python: requires >= 3.11 (uses dict.fromkeys, zip(strict=True), structural pattern matching).
#
# The floors below are the Python 3.11-era minima. They are NOT installable minima on the
# newer interpreters this project also runs on: numpy 1.26 / pandas 2.2 / scipy 1.13 ship no
# CPython 3.13+ wheels, so on Python 3.13/3.14 a lowest-version resolver (e.g. `uv pip install
# --resolution=lowest`) would fail to build. Ordinary `pip install -r requirements.txt`
# resolves to the newest compatible wheel and works on 3.11-3.14 — that is the supported path.
#
# Tested on Python 3.14.2 with: numpy 2.4.3, pandas 2.3.3, scipy 1.17.0, scikit-learn 1.8.0,
# matplotlib 3.10.8, seaborn 0.13.x. For byte-identical figure regeneration, pin to those exact
# versions (matplotlib in particular: minor-version bumps shift figure bytes without changing
# pixels — the shipped snapshot was rendered with matplotlib 3.10.8, not 3.9.x).
pandas>=2.2
numpy>=1.26
scipy>=1.13
scikit-learn>=1.5
matplotlib>=3.9,<3.11
seaborn>=0.13
requests>=2.32
pycountry>=24.0
pyreadr>=0.5
openpyxl>=3.1  # read the bundled TOP500 .xlsx list (extract_top500.py)
jupyter>=1.0
nbconvert>=7.16
